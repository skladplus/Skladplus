# 02 — Решения

Единый журнал: решение · обоснование · дата. Отвергнутые варианты записаны вместе
с принятыми — иначе через месяц их начнут пересматривать по кругу.
ADR отдельными файлами не ведём: при двух разработчиках один список читается быстрее.

## Продукт и организация работы

| Решение | Обоснование | Дата |
|---|---|---|
| Свой код на Next.js + Postgres, не low-code | Lovable и Base44 не дают контроля над мультиклиентским разграничением доступа и биллингом; переписывать пришлось бы целиком | 2026-08-22 |
| Этап 0 — только каркас, без схемы БД и UX | Бизнес-правила не закрыты, часть ответов есть только у заказчика; написанная сейчас схема была бы переписана. Репозиторий, процесс и защита от этих ответов не зависят | 2026-08-22 |
| Организация `skladplus`, оба владельца | Личный репозиторий одного человека — единая точка отказа и неравные права при заявленном паритете 50/50 | 2026-08-22 |
| Репозиторий публичный | CodeRabbit, защита веток, CODEOWNERS и минуты Actions на публичном репозитории бесплатны. Код защите не подлежит — защищаем секреты, доступы и данные | 2026-08-22 |
| Free-организация, не Team | На публичных репозиториях Team не даёт ничего сверх Free | 2026-08-22 |
| Файла `LICENSE` нет | Отсутствие лицензии = все права защищены. Открытый исходный код не означает разрешения на использование | 2026-08-22 |
| Тарифы заказчика публикуем с числами | Приняли осознанно: условия видны конкурентам, ответ на вопрос заказчика готовим заранее | 2026-08-22 |

## Процесс

| Решение | Обоснование | Дата |
|---|---|---|
| Апрув второго человека на merge не обязателен | Двое, работают параллельно; обязательный апрув превратился бы в очередь. Гейт — зелёный `ci-ok` и CodeRabbit | 2026-08-22 |
| Прямой push в `main` закрыт для всех, `bypass_actors: []` | Исключение для владельца обесценивает защиту: обходить будут в спешке, а спешка и есть источник сломанного `main` | 2026-08-22 |
| Только squash-merge | Линейная история; merge-коммиты и rebase отключены на уровне репозитория, а не договорённости | 2026-08-22 |
| `squash_merge_commit_message = BLANK` | При PR из одного коммита GitHub предзаполняет тело PR телом коммита — запрещённый AI-трейлер вернулся бы этим путём | 2026-08-22 |
| `strict_required_status_checks_policy: false` | При двух параллельных писателях strict сериализует мержи: каждый чужой merge сбрасывает готовность второго. Вместо него `allow_auto_merge` | 2026-08-22 |
| Один обязательный чек — агрегат `ci-ok` | Иначе новый job не станет обязательным автоматически, а переименование старого молча откроет мерж | 2026-08-22 |
| `paths:` и `paths-ignore:` в CI запрещены | Отфильтрованный обязательный чек навсегда остаётся `pending` и блокирует PR | 2026-08-22 |
| Отвергнут commitlint | Conventional Commits соблюдаются двумя людьми без робота; ещё один красный чек не окупается | 2026-08-22 |
| AI в авторстве запрещён | Требование заказчика работ: контрибьюторы — только люди. Механизм: `includeCoAuthoredBy: false` + правило в `AGENTS.md` для Codex + job `authorship` как страховка. `git config` этим не управляет — трейлер добавляет агент, а не git | 2026-08-22 |
| Каркас делает один человек, не оба | День-полтора работы; координация двоих съела бы больше, чем сэкономила | 2026-08-22 |
| После каркаса — trunk-based, ветки на часы, stacked PR, флаги, worktrees | Узкое место при работе с агентами — не написание кода, а ревью и интеграция. Выигрыш даёт сокращение времени жизни ветки, а не рост их числа | 2026-08-22 |
| Домен делится по ограниченным контекстам | Товары/поставки/остатки одному, заказы/отправки/возвраты другому, каждый от таблицы до экрана. Деление по слоям заставляло бы ждать друг друга | 2026-08-22 |

## Инструменты

| Решение | Обоснование | Дата |
|---|---|---|
| Biome вместо ESLint + Prettier | Один конфиг вместо двух, нет конфликта форматтера с линтером, оба IDE форматируют одинаково | 2026-08-22 |
| TypeScript 7.0.2 (нативный компилятор) | Уже `latest`, не rc; `tsc --noEmit` проверен на скелете. При проблеме откат на 5.9 — одна строка | 2026-08-22 |
| pnpm 10.34.5, не 11 | 11 вышел недавно; выгоды нет, а Vercel corepack и `pnpm/action-setup` на 10 проверены | 2026-08-22 |
| `packageManager` + corepack, версия пинуется точно | `pnpm/action-setup` без поля `version` берёт её отсюда; указание версии в двух местах даёт конфликт | 2026-08-22 |
| Все скрипты корня — `pnpm -r --if-present` | CI зелёный на пустом монорепо и остаётся зелёным по мере появления пакетов. Без этого CI не мог бы позеленеть до появления приложения, а приложение не могло бы появиться до зелёного CI | 2026-08-22 |
| CI ставит зависимости с `--ignore-scripts` | От карантина на свежие релизы отказались, поэтому это единственный барьер против вредоносного `postinstall` на раннере с токенами. Нужные сборки вызываются явными шагами | 2026-08-22 |
| `pnpm dlx` и `npx` запрещены | Резолвят latest на каждом прогоне: невоспроизводимая сборка и открытый вход для supply-chain | 2026-08-22 |
| gitleaks бинарём, не экшеном | Экшен требует лицензию для организаций | 2026-08-22 |
| Отвергнут gitleaks в pre-commit | Обходится `--no-verify`; реально гарантирует только серверный push protection, который включён | 2026-08-22 |
| Отвергнут Redis в docker-compose | Воркера ещё нет; сервис без потребителя только ломает старт | 2026-08-22 |
| Vercel + Neon, не свой сервер | Двое разработчиков; администрирование сервера — третья работа, которой нет | 2026-08-22 |
| Каркас обязан работать на локальном Postgres без Vercel и Neon | Иначе второй разработчик заблокирован чужими доступами | 2026-08-22 |
| Neon: отдельный проект, не общий с NovaPostBot | Общая база двух продуктов разных клиентов — ошибка разграничения доступов и бэкапов | 2026-08-22 |
| Миграции отдельным джобом на `push: main`, не внутри `next build` | Сборка Vercel параллельна и кэшируется — получились бы две миграции на одной базе. Джоб с `concurrency: deploy-main` | 2026-08-22 |
| Neon MCP видит только ветку с синтетикой | Прод-данные не должны попадать в контекст агента ни при каком стечении обстоятельств | 2026-08-22 |

## Код

| Решение | Обоснование | Дата |
|---|---|---|
| Деньги — целые копейки, `bigint` | Prisma по умолчанию даст `Float`, и это проскочит незамеченным до первого расхождения в счёте. `Decimal.js` не сериализуется через границу server/client в Next.js | 2026-08-22 |
| Время — `timestamptz`, вывод через `Intl.DateTimeFormat('uk-UA', {timeZone:'Europe/Kyiv'})` | Рантайм Vercel — UTC, переопределить `TZ` там нельзя. Иначе локальная машина в киевской зоне даст зелёные тесты при красном проде, и всплывёт это на SLA «30 рабочих минут» | 2026-08-22 |
| Доступ к данным — обёртка-репозиторий с обязательным `accountId` | Доступ к чужим заказам — риск номер один в мультиклиентской системе. Закладывается пока каркас пуст: час работы сейчас против унификации разъехавшихся реализаций потом. Слабое место честное — обходится прямым вызовом Prisma; дисциплину держит линтер и то, что доступ живёт в одном месте | 2026-08-22 |
| Интерфейс украинский, комментарии и документы русские | Как в NovaPostBot | 2026-08-22 |
| `packages/core` не импортирует React и Next | Иначе бизнес-логика прирастает к фреймворку и не тестируется отдельно | 2026-08-22 |

## Память и правила

| Решение | Обоснование | Дата |
|---|---|---|
| `AGENTS.md` — канон, `CLAUDE.md` — указатель | Codex читает только `AGENTS.md`; правило, попавшее лишь в `CLAUDE.md`, для второго разработчика не существует | 2026-08-22 |
| Лимит `AGENTS.md` — 150 строк, проверяется в CI | Длинный файл работает хуже короткого: середина теряется. Детали выносятся в `docs/` со ссылкой | 2026-08-22 |
| Единственная общая память двух агентов — файлы в репозитории | Персональная память Claude не лежит в git и второму недоступна | 2026-08-22 |
| `scripts/context.sh` — один ввод в контекст для обоих | У Claude висит на `SessionStart`, у Codex вызывается по правилу «шаг 0». Один источник, никакого дублирования | 2026-08-22 |
| Журналы раздельные: `PROGRESS/alex.md` и `PROGRESS/step.md` | Общий файл давал бы конфликт в каждом PR | 2026-08-22 |

## Принятые риски

Это решения, а не упущения.

1. **Автодеплой из `main` без человеческого гейта.** Зелёный CI не защищает от автора, который пишет и код, и тесты. Страховка — CodeRabbit и откат за 30 минут.
2. **PR, правящий `.github/`, проверяется уже изменённым workflow** — гейт проверяет сам себя. Реализуется только при компрометации аккаунта одного из двоих.
3. **Bus factor за пределами организации не закрыт:** нет офлайн recovery-кодов, второго админа в Neon, Vercel и Sentry, зеркала репозитория. Два владельца организации закрывают главный сценарий, остальные остаются.
4. **Без карантина на свежие npm-релизы.** Барьер — только `--ignore-scripts` в CI.
5. **Публичный репозиторий с тарифами заказчика.**
6. **Deny-правила агентов — не граница, а трение.** Запрет `curl` обходится через `node -e`; запрет чтения `.env` — через `cat` и `grep -r`. Реально работает вынос секретов за пределы рабочей папки, всё остальное лишь удорожает случайную ошибку.

## Уточнения по факту исполнения (2026-08-22)

План писался до применения; ниже то, в чём реальность разошлась с ним.

| Пункт плана | Как оказалось |
|---|---|
| «Добавить `admin:org`, без него организация не создаётся» | Организацию нельзя создать через API вообще: `POST /admin/organizations` существует только в GitHub Enterprise. Для github.com создание org доступно лишь через веб. `admin:org` нужен не для создания, а для приглашений и проверок после |
| pnpm `10.x.y` | Взят 10.34.5. Актуальный `latest` — 11.22.0, но выгоды нет, а связка Vercel corepack + `pnpm/action-setup` на десятке проверена |
| TypeScript «5.x» подразумевался | `latest` уже 7.0.2 — нативный компилятор, не rc. Взят он, `tsc --noEmit` проверен на скелете |
| Ruleset задаётся ровно перечисленными правилами | GitHub добавил от себя `require_extra_approval_for_unattributed_changes: true`. Оставлено: неатрибутированные коммиты потребуют апрува, что совпадает с правилом «в контрибьюторах только люди» |
| Расширенные паттерны secret scanning | `secret_scanning_non_provider_patterns` и `validity_checks` API принимает и молча игнорирует — на этом тарифе недоступны. Базовый push protection работает |
| Проверка CODEOWNERS через `codeowners/errors` | На личном репозитории эндпоинт отдаёт 404: `@Stepandj` не имеет доступа, файл не парсится. Проверяется заново после переноса в организацию |
| Джоб `authorship` — единственная ловушка самоцитирования | Их оказалось три. Правило против `dlx`/`npx` срабатывало на `scripts/context.sh`, который печатает текст запрета, и затем на собственном сообщении об ошибке. Решение общее: искать команду в позиции запуска, а не вхождение строки, и отбрасывать комментарии |
| Deny-by-default `.gitignore` перечисляет разрешённое | При добавлении `.coderabbit.yaml` он оказался отрезан и потребовал явного `!`. Это не дефект, а цена подхода: каждый новый файл в корне придётся разрешать вручную |

## Уточнения по факту исполнения, часть вторая (2026-08-22)

Найдено при переносе в организацию и постройке приложения.

| Что | Как оказалось |
|---|---|
| Перенос репозитория в организацию | **Сбросил secret scanning и push protection в `disabled`.** На личном репозитории они были включены; Free-организация ставит их выключенными по умолчанию для входящих репозиториев. Возвращено вручную, плюс выставлены org-настройки по умолчанию, чтобы следующий репозиторий не наступил на то же |
| Проверка `codeowners/errors` | Эндпоинту нужен **полный ref**: `?ref=refs/heads/main` отдаёт `{"errors":[]}`, а без него и с `?ref=main` — 404. Файл валиден |
| CODEOWNERS как «авто-запрос ревью» | Не работает. События `review_requested` не возникает: авто-запрос включается правилом `require_code_owner_review`, а оно делает апрув обязательным — то есть создаёт очередь, от которой отказались. Уведомление вынесено в отдельный workflow `review-request.yml`, который ничего не блокирует. CODEOWNERS оставлен как запись о владении |
| Снятие scope `gist` | Невозможно: `gh auth refresh --remove-scopes` сообщает, что `repo`, `read:org` и `gist` — минимальный набор gh CLI. Снят только `delete_repo`. Остаётся deny-правило `Bash(gh gist:*)` у агента — это трение, а не граница |
| Организация создаётся через API | Нельзя: `POST /admin/organizations` есть только в GitHub Enterprise. Создано через веб |
| Порт локального Postgres | 5432 занят базой другого проекта на той же машине. Skladplus слушает **5433**, переопределяется `SKLADPLUS_DB_PORT` |
| Prisma 7: `url` и `directUrl` в схеме | Удалены из схемы, переехали в `prisma.config.ts`; рантайм получает driver adapter (`@prisma/adapter-pg`). Замысел плана сохранён: миграции ходят только по прямому подключению |
| Строгий `env()` из `prisma/config` | Роняет **любую** команду при отсутствии переменной, включая `prisma generate` на сборке Vercel, где `SHADOW_DATABASE_URL` нет и быть не должно. Используется `process.env` |
| `prisma migrate diff` и shadow-база | Флаг `--shadow-database-url` в Prisma 7 убран, значение берётся из конфига. Первая редакция скрипта указывала туда рабочую базу — Prisma пересоздаёт shadow при каждом запуске, то есть команда снесла бы рабочие данные. Заведена отдельная `skladplus_shadow` |
| Генерация клиента Prisma в CI | Клиент не коммитится, поэтому нужен **каждому** джобу, а не только сборке. Первый прогон уронил `typecheck` и `unit`; локально они проходили на клиенте от прошлых запусков — ровно тот случай, когда зелёное локально ничего не значит. Генерация перенесена в общий шаг установки |
| Плагины пинуются по `gitCommitSha` | Не сделано осознанно. Пин официального маркетплейса Anthropic заморозил бы и обновления безопасности всех 286 плагинов, а механизма их отслеживания, вроде Dependabot, нет. Решение пересмотреть, если появится сторонний маркетплейс с плагинами в работе |
| Сторонний маркетплейс `data-goblin/power-bi-agentic-development` | Оставлен: все 10 его плагинов отключены, а сам он может использоваться в других проектах владельца машины. Зафиксировано как осознанное решение, а не упущение |

### Новый принятый риск

7. **Второй владелец организации без двухфакторной аутентификации.** У `@Stepandj`
   2FA выключена, а прав у него столько же, сколько у первого владельца: он может
   снять ruleset, выключить push protection и вычистить организацию. Требование 2FA
   на уровне организации не включено намеренно — оно немедленно исключило бы его
   из организации. Это самое слабое место всей защиты: ruleset, `bypass_actors: []`
   и push protection обходятся тем, кто владеет учётной записью.

### Override на `deepmerge-ts` (2026-08-22)

Dependabot нашёл HIGH в `deepmerge-ts@7.1.5` — исчерпание стека при слиянии
рекурсивных объектов. Пакет приходит транзитивно из `@prisma/config@7.9.1`,
и версии Prisma без него нет: 7.9.1 — последняя.

Практический риск для нас околонулевой: `@prisma/config` сливает наш собственный
конфиг, который не приходит извне и рекурсивным быть не может. Но висящий HIGH
приучает не смотреть на алерты, поэтому поднят через `pnpm.overrides` до 8.0.2.

Мажорный апгрейд транзитивной зависимости — риск сам по себе, поэтому проверен
весь конвейер, а не только сборка: `generate`, `deploy`, `status`, `diff` (включая
ненулевой код при дрейфе), типы, тесты, сборка. Override снимается, когда Prisma
подтянет восьмёрку сама.

### Postgres 18 (2026-08-22)

Локально, в CI и на Neon — одна мажорная версия. Расхождение здесь даёт зелёные
тесты при красном проде, как и расхождение часовых поясов. Выбрана 18, а не 17:
горизонт поддержки длиннее, а день ноль — последний момент, когда смена мажора
ничего не стоит.

При переходе всплыло: **образ `postgres:18` сменил точку монтирования.** Том
вешается на `/var/lib/postgresql`, а не на `/var/lib/postgresql/data` — данные
раскладываются по версионным подкаталогам, чтобы работал `pg_upgrade --link`.
Со старой точкой контейнер уходит в цикл перезапуска, и это не видно из
`docker compose up`: нужен `docker logs`. В CI сервис-контейнер тома не имеет,
поэтому там проблема не проявилась бы — она ждала бы разработчика на его машине.

## Neon: как заведён и почему так (2026-08-22)

Проект `Skladplus`, `aws-eu-central-1` (Франкфурт — регионов ближе к Украине
у Neon нет), Postgres 18, тариф Launch. Launch выбран потому, что он больше
не стоит фиксированных денег: чистый pay-as-you-go без базовой платы, при нашей
нагрузке $3–5 в месяц. За это — окно восстановления 7 дней вместо 6 часов
на Free и защищённые ветки.

**Поправлено после создания:** ветка `production` переведена в protected
(создаётся незащищённой), автоскейл снижен с 8 CU до 1 — и в настройках
по умолчанию, и на уже созданном эндпоинте, потому что первое действует
только на новые.

### Границы доступа агента

План требовал «Neon MCP видит только ветку с синтетикой». **Такой границы
в Neon не существует:** ключи бывают персональные, организационные
и project-scoped; ветка областью действия быть не может.

Это важнее, чем кажется, потому что боевая база другого клиента (NovaPostBot)
лежит **в той же организации Neon**. OAuth-авторизация MCP — кнопка
«Authenticate» в `/mcp` — выдаёт доступ ко всему аккаунту, то есть к его проду.
Онбординг Neon вдобавок сам прописал такой сервер в user-scope, где он
действовал бы во всех проектах машины; он удалён.

Работающая граница — **project-scoped ключ**. Проверено с трёх сторон:
свой проект открывается, чужой отдаёт `project not found`, список проектов
организации — `not allowed to perform actions outside the project this key
is scoped to`.

Ключ хранится только в `~/.skladplus/env` (0600). В конфиг MCP записана
переменная `${NEON_API_KEY}`, не значение. Пока в базе синтетика, проекта
как границы достаточно; перед первым боевым клиентом — пункт 45
в `docs/findings-backlog.md`.

При выпуске ключа первая попытка напечатала его в вывод команды. Ключ
немедленно отозван и выпущен заново, вывод второй попытки в терминал
не попадает. Записано здесь, потому что «секрет мелькнул в логе» —
это скомпрометированный секрет, а не досадная мелочь.

### Мастер `neon init --agent` не применялся

Он выдаёт агенту инструкции, которые ломают четыре наших решения:
`npm install` в pnpm-воркспейсе, установка пакетов через `npx -y` (запрещено
как неповторяемая сборка), `neon env pull` со строкой подключения в файл
внутри репозитория (секреты живут вне рабочей папки) и `@neondatabase/serverless`
вторым путём доступа к базе мимо `@prisma/adapter-pg`. Завершается он
установкой Neon Auth — архитектурным решением, которого нет в плане.

Инструкции прочитаны в песочнице как данные и не выполнены. Правило про
такие мастера добавлено в `AGENTS.md`: Codex получит от Neon ровно тот же текст.

### Строки подключения к Neon локально не хранятся

Локальная разработка идёт на Postgres из compose. Прод и preview получают
строки от нативной интеграции Neon↔Vercel, которая заводит ветку БД на каждый
PR и пишет переменные сама. Проставленные руками потом конфликтуют с её
значениями, поэтому интеграция ставится **до** первого preview-деплоя.

## Границы доступа агентов к внешним сервисам (2026-08-22)

Общее правило: **MCP подключается только там, где доступ можно ограничить.**
Где ограничить нельзя — сервис используется через CLI с узким токеном либо
через дашборд руками.

| Сервис | Решение |
|---|---|
| **Neon** | Подключён. Ключ ограничен проектом Skladplus. Проверено: свой проект открыт, `list_projects` отдаёт `not allowed for organization API keys`, чужой проект недоступен |
| **Vercel** | **Не подключаем.** Документация Vercel прямо говорит: подключение MCP выдаёт агенту тот же доступ, что у учётной записи пользователя. Ограничения по проекту или команде нет, OAuth без альтернативы. Настройка — через дашборд, остальное — `vercel` CLI с токеном, ограниченным командой |
| **Sentry** | Не подключаем на этапе 0: проекта в Sentry ещё нет, а токен читает ошибки по всему аккаунту |
| **Prisma-Remote** | Не подключаем: он про Prisma Postgres, которым мы не пользуемся. Локальный `Prisma-Local` работает и достаточен |
| **GitHub** | Не подключаем: MCP требует подписки Copilot, а `gh` CLI закрывает всё то же самое и уже используется |
| **context7, playwright, semgrep** | Подключены: доступа к нашим данным не имеют |

Отдельно: `desktop-commander` из user-scope запускается как `npx -y ...@latest`,
то есть тянет свежий пакет при каждом старте и даёт широкий доступ к файловой
системе и терминалу. Это тот же запрещённый паттерн, что `pnpm dlx`, и открытый
вход для supply-chain. К Skladplus отношения не имеет и подлежит удалению.

## Vercel: сборка монорепо (2026-08-22)

Настройки сборки живут в `apps/web/vercel.json`, а не только в дашборде:
`vercel.json` перекрывает настройки проекта, версионируется и виден второму
разработчику. В дашборде остаётся то, что оттуда задать нельзя: Root Directory,
переключатель «Include source files outside of the Root Directory» и переменные
окружения.

Команда сборки обязана начинаться с генерации клиента Prisma. Клиент
не коммитится, а `next build` его не создаёт — на Vercel это даёт ту же ошибку
`Cannot find module './generated/client'`, что уронила первый прогон CI.
Форма `pnpm --filter @skladplus/db db:generate` выбрана потому, что бинарь
`prisma` лежит в зависимостях `packages/db`, а не `apps/web`: при изолированных
`node_modules` pnpm прямой вызов `prisma` из `apps/web` не разрешается.

Отдельно про кэш: при попадании в кэш сборки Vercel пропускает установку
зависимостей, поэтому генерация обязана быть шагом сборки, а не постустановочным
скриптом — иначе на прод уедет устаревший клиент.

## Vercel после подключения интеграции Neon (2026-08-22)

Проверено через API, а не по экрану настроек.

| Что | Было | Стало |
|---|---|---|
| Регион функций | `iad1` — Вашингтон | `fra1` — Франкфурт |
| Имя прямого подключения | ждали `DIRECT_URL` | интеграция даёт `DATABASE_URL_UNPOOLED` |

**Регион — не косметика.** База в `aws-eu-central-1`, а функции по умолчанию
поднялись в Вашингтоне: каждый запрос Prisma ходил бы через Атлантику, и это
попало бы прямо в SLA «30 рабочих минут» — только заметили бы не сразу,
а на живых заказах.

**Имя переменной** подстраиваем мы, а не интеграция. Она перезаписывает свои
значения при каждом деплое, поэтому ручное переименование не переживёт первый
же пуш. `prisma.config.ts` принимает оба имени: локально и в CI — `DIRECT_URL`,
на Vercel — `DATABASE_URL_UNPOOLED`.

Подтверждено также: `sourceFilesOutsideRootDirectory: true` (иначе pnpm-воркспейс
не собрался бы), `nodeVersion: 24.x` совпадает с `.nvmrc`, `gitForkProtection`
и `protectedSourcemaps` включены, проект непубличный — логи сборки посторонним
не видны.

## Миграции прода (2026-08-22)

Проверка после первого успешного деплоя показала: боевая ветка Neon **пуста —
ни одной таблицы, включая `_prisma_migrations`**. Миграции туда никогда
не применялись. Джоб `db` в CI гоняет их против сервис-контейнера, а не против
Neon, и этого достаточно для проверки конвейера, но не для прода.

Пока единственная миграция пустая, это безвредно. Опасно другое: механизма
не было вовсе, и первая же настоящая миграция не доехала бы до прода молча —
приложение пошло бы работать против схемы, которой нет.

Заведён отдельный workflow `migrate.yml` на `push: main`:

- `concurrency: deploy-main` с `cancel-in-progress: false`. Прерывать миграцию
  на полпути нельзя: база останется в промежуточном состоянии, а
  `_prisma_migrations` запомнит её незавершённой.
- Отдельно от сборки Vercel: та параллельна и кэшируется, из-за чего две миграции
  могли бы поехать на одну базу.
- Явная проверка, что секрет задан: без неё пустая переменная дала бы
  «нечего применять» и зелёный прогон при неприменённых миграциях.
- `migrate status` до и после — в логе видно, что именно поехало.

Строка подключения лежит в секрете репозитория `NEON_PRODUCTION_DIRECT_URL`,
прямая (без `-pooler`): pooled-соединение миграции не переживёт. Репозиторий
публичный, но секреты недоступны PR из форков, а `push` в `main` закрыт
ruleset'ом.

**Незакрытое:** миграция и деплой кода идут параллельно, поэтому новый код может
доехать раньше своей миграции. Пока схема пуста, это не проявляется; лечится
дисциплиной expand/contract — пункт 37 в `docs/findings-backlog.md`.

## Обход защиты деплоев для автоматизации (2026-08-22)

На проекте включена Vercel Authentication: и preview, и production отдают 302
всем, кроме авторизованных участников команды. План собирался включить её
«перед первым боевым клиентом» — она включена сразу, и это лучше.

Побочный эффект: проверить `/api/health` снаружи нельзя, и Playwright не сможет
гонять тесты против preview (задача #12). Для этого у Vercel есть штатный
механизм — Protection Bypass for Automation: запрос с заголовком
`x-vercel-protection-bypass: <секрет>` проходит мимо защиты.

Секрет включён и лежит **только** в `~/.skladplus/env` под именем
`VERCEL_AUTOMATION_BYPASS_SECRET`. В репозиторий он не попадает. Для CI его
нужно будет положить в секреты репозитория, когда появится e2e-джоб против
preview.

Проверка прода командой:

```bash
set -a; . ~/.skladplus/env; set +a
node -e 'fetch("https://<деплой>.vercel.app/api/health",{headers:{"x-vercel-protection-bypass":process.env.VERCEL_AUTOMATION_BYPASS_SECRET}}).then(r=>r.text()).then(console.log)'
```

**Это секрет ровно той же силы, что пароль от деплоя:** кто им владеет, тот
видит preview и production в обход авторизации. Хранится как остальные —
вне рабочей папки, права 0600. Отзывается и перевыпускается в настройках
проекта на Vercel.

### CodeRabbit: установки GitHub App недостаточно

Приложение `coderabbitai` установлено в организацию с доступом ко всем
репозиториям — это видно через `gh api orgs/skladplus/installations`. Но ревью
не появлялось ни на одном PR, и прямой вызов `@coderabbitai review` тоже остался
без ответа. У CodeRabbit собственный тумблер на репозиторий в его дашборде,
и без него сервис за репозиторием не следит.

Проверять надо не установку приложения, а факт ревью на живом PR: установка
выглядит как «готово» и ничего не делает.

Отсюда же правило в `AGENTS.md`: PR не мержится раньше, чем ответил CodeRabbit.
Все PR этапа 0 мержились через минуту после открытия — ревью не успело бы,
даже будь оно включено.
