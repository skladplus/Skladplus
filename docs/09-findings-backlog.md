# 09 — Отложенные находки

Домен, который сознательно не решается сейчас: каркас от него не зависит.
Файл живёт в репозитории, чтобы попадать агентам в контекст и быть видимым обоим.
Пункт закрывается переносом в `docs/06-architecture.md` или в схему.
`#N` здесь и в остальных документах — номер пункта; ссылка на GitHub идёт
со словом: `PR #23`, `issue #16`.

**Приоритеты.** `П1` — блокирует проектирование схемы БД, нужен до первой таблицы.
`П2` — нужен до первого боевого клиента. `П3` — можно решать по ходу.

## Зафиксировано, ждёт реализации

Ответы получены, решения приняты; в код не заведены, потому что этап 0 — каркас.

| # | Решение |
|---|---|
| 1 | Комиссия за сборку: 20 грн за заказ + 1 грн за каждую единицу сверх первой, **по фактически собранному** составу, а не по заказанному |
| 2 | Хранение 385 грн/м³ в месяц, объём — **среднее за период по ежедневным снимкам остатков** |
| 3 | Приёмка 1 грн/ед · возврат 12 грн/заказ · листовка 2,5 грн · проверка 2 грн/шт · фотоотчёт 4 грн |
| 4 | SLA — 30 **рабочих** минут от прихода вебхука; финиш — отметка менеджера «передан перевозчику» с последующим подтверждением сканом Новой Почты. Двухстадийность обязательна: скан приходит позже реальной передачи |
| 5 | Рабочее время 8:00–20:00 ежедневно, `Europe/Kyiv`, задаётся в dev-конфиге и в UI не настраивается |
| 6 | Тариф индивидуальный для каждого клиента, версионный, с датами действия; цена фиксируется **в проводке** в момент операции, а не берётся из справочника задним числом |
| 7 | Штрафной ноль по SLA (обработка бесплатна) действует с первого дня |
| 8 | Склад один, адрес — тот же, что в NovaPostBot |
| 9 | Адресное хранение: ячейки закладываются в схему сразу, экраны позже |
| 10 | Остаток резервируется в момент приёма заказа. Ситуации «заказ собирается, товара нет» не существует по построению |
| 11 | Один заказ — строго одна ТТН |
| 12 | ТТН создаётся ключом клиента (его ФОП), не ключом склада |
| 13 | Заказы из кабинета и из CRM равнозначны; подключение CRM (SalesDrive, KeyCRM, Хорошоп) — опция клиента, не базовый путь |
| 14 | Клиент заводится саморегистрацией с подтверждением менеджером |
| 15 | Документооборот полноценный: акты приёмки, акты расхождений, претензии |
| 16 | Страхование партии на 150 000 грн |
| 17 | Доп. услуги оформляются заявками от клиента, а не включаются молча |
| 18 | Данные получателя — отдельная таблица, чтобы её можно было обезличить, не трогая журналы |
| 19 | Оплата постоплатой по счёту за период — окончательно (#55); расхождение с предоплатным балансом из видео закрыто |
| 20 | Объём — до 15 000 заказов в месяц со всех клиентов |
| 21 | Роли (уточнено 2026-08-26): владелец склада · менеджер(ы) склада — вся операционка, включая сборку: отдельных ролей сборщика и руководителя сборки не будет · клиент (магазин) · сотрудники команды клиента с пресетами прав (#65). Разработчики — сознательно не роль в БД. Детальная модель — `docs/02-product-spec.md` |

## Решено

Источники: транскрипт видео-прохода заказчика (2026-08-22) и словесный разбор
структуры референсной системы (сами материалы — вне git, правило №3 `AGENTS.md`).
`↷` — требуется подтверждение заказчика.

| # | Решение |
|---|---|
| 22 | Возвраты — два типа: відмова (посылку не забрали) и повернення після вручення. Разные потоки и тарифы отдельными строками тарифа; 12 грн/заказ — дефолт обоим типам ↷ подтвердить цифру |
| 23 | Клиент видит два числа остатка: фізичний и доступний (за вычетом резерва) |
| 24 | Комплекты полностью в MVP: состав компонентов, экран состава, резерв и списание компонентов при сборке |
| 25 | Инвентаризация: экран «системное количество vs фактическое» в складском контуре MVP, ввод ручной (сканер отменён — #84); расхождение — корректировка отдельным типом движения в журнале остатков |
| 47 | Комиссия менеджера — процент от комиссии клиента за собранный заказ; ставка версионная, задаётся владельцем в UI, дефолта нет — без введённой ставки комиссия не начисляется. Иерархии сборщиков не будет (#21) |
| 48 | Паузу SLA («Проблема»/«Призупинити») менеджер ставит сам, комментарий обязателен; SLA и комиссия не сгорают; владелец видит журнал пауз и может аннулировать неправомерную задним числом |
| 49 | Статусы заказа: полная таксономия референсной системы (17 статусов) + черновик; черновые украинские подписи утверждены, правятся по факту замечаний |
| 50 | «Віртуальний товар» — позиция без физического остатка (под заказ, услуга-довесок): добавляется в заказ при нулевом складе, без резерва, подбора с полки и тарифов хранения/приёмки |
| 51 | Низкий остаток: счётчик «потребує уваги» на дашборде + email-дайджест по настраиваемому клиентом расписанию (частота и дни недели меняются в настройках) |
| 52 | «Дні без руху»: рух = отправка в заказе; порог настраивается в карточке товара, дефолт 30 дней |
| 53 | Наложенные платежи в дороге: фоновый опрос API Новой Почты раз в час в рабочее время + кнопка ручного обновления (архитектура воркера — #36) |
| 54 | Поддержка — встроенный чат в кабинете, в MVP: клиент ↔ дежурный менеджер, история сохраняется; «менеджер на зміні» — настройка склада |
| 55 | Финансовая модель — постоплата по счёту (#19 подтверждён) — окончательно; расхождение с видео заказчика закрыто |
| 56 | Черновик заказа (DRAFT) в машине статусов: не резервирует остаток, складу не виден |
| 57 | «Помилки в замовленнях» на дашборде = технические ошибки (не создалась ТТН, невалидный адрес или телефон, отклонённый вебхук) + брак, обнаруженный при сборке |
| 58 | Объём товара для тарифа хранения (385 грн/м³, #2) фиксирует склад замером при приёмке поставки |
| 59 | (наш, П3) Карта статусов трекинга Новой Почты во внутренние статусы — свести при реализации воркера |
| 82 | Наблюдаемость: Sentry (free) + внешний uptime-чек `/api/health` с bypass-заголовком и алертом в Telegram; заводится вместе с воркером (#36) — его таблица прогонов закрывает «джоб умер молча». До этого — логи Vercel руками |
| 83 | Retention: фото приёмки, возвратов и списаний — 6 месяцев после закрытия объекта; ПДн получателя — обезличивание (#18) через 6 месяцев после доставки; исполняет воркер. Журналы бессрочны (05 §6): ПДн в них нет — идентификаторы и операционные комментарии, ПДн в комментарий не пишутся (подсказка поля + ревью экранов) ↷ подтвердить сроки заказчику |
| 84 | Складской контур — desktop-first: менеджер работает за компьютером, сканер штрих-кодов и печать этикеток не используются — собранную посылку несут в отделение НП, этикетку печатает отделение по созданной системой ТТН. Отменяет сканер в #25; сканер-паттерн, print-роут этикетки и tablet-first уходят из 02 и 03 отдельным PR |
| 85 | Недоступность API НП не держит сборку: заказ завершается без ТТН, она добирается кнопкой после восстановления, пауза — «не з нашого боку» (#48); при потере сети на складе — блокирующий баннер, офлайн-режим не строим |
| 26 | Повторная отправка после невыкупа — новый заказ со ссылкой `reshipmentOfId?` на исходный; сборка повторяется, комиссия начисляется снова ↷ подтвердить вторую комиссию |
| 27 | Отмена заказа бесплатна до передачи перевозчику; после передачи — тариф возврата по типу (#22) и доставка по факту ↷ подтвердить бесплатность работ склада при отмене в сборке |
| 28 | Неоплата счёта ничего не блокирует автоматически: просрочка подсвечивается, меры — решение владельца руками ↷ подтвердить отказ от автоблокировок |
| 29 | Компенсация за утрату и повреждение на складе — индивидуально по договору, вне системы; платформа фиксирует итог корректирующей проводкой владельца с комментарием |
| 30 | Отчёты владельца: стартовый набор — обороты и комиссии по клиентам · SLA и штрафные нули · комиссии менеджеров · занятость склада м³ · динамика заказов; расширение реестром виджетов ↷ показать набор заказчику |
| 31 | Уведомления по SLA: за 10 рабочих минут до дедлайна — Telegram владельцу и дежурному менеджеру (канал общий с алертами #82); в UI — цвет приоритета |
| 32 | Частичная сборка разрешена с согласия клиента: отправка неполного состава с пометкой, остаток — доотправка новым заказом (#26) |
| 33 | Списание брака инициирует менеджер из приёмки, инвентаризации или возврата; фото и комментарий обязательны, движение WRITE_OFF, клиент уведомляется |
| 34 | Хранение сверх срока не автоматизируется: невостребованный товар копит помесячный счёт; утилизация — договорённость вне системы, фиксируется списанием с фото |
| 35 | Приоритет при пиковой нагрузке — FIFO по дедлайну SLA (сортировка по остатку рабочего времени), приоритетных клиентов нет |
| 86 | Лимит Excel-импорта остаётся 10 МБ: файл уходит по подписанному URL в хранилище (#38), обработка — воркером (#36); спека 02 §4.2.5 не меняется, узкое место 05 §7 №6 закрыто |
| 87 | Интервалы поллинга на старте: очередь 10 с · чат 5 с · дашборды 30 с — настройка PLATFORM, меняется без деплоя |
| 60 | Упаковка («пакування») — отдельная сущность с остатками и списанием в заказ, в MVP |
| 61 | Фискализация и товарный чек — после MVP |
| 62 | СМС получателям о статусах — после MVP |
| 63 | Рейтинг выкупа получателя — после MVP; затем считать по собственной истории заказов платформы |
| 64 | Публичный API для клиентов (заказы/товары/поставки по ключу, документация в кабинете) — после MVP; zod-схемы и так общие для форм и API |
| 65 | Права сотрудников команды клиента — три пресета: Власник · Оператор (без финансов и настроек) · Перегляд |
| 66 | Доступ персонала платформы (`owner`, `manager`): 2FA обязательна перед первым боевым клиентом (в связке с #45), TTL сессий, журнал входов. На команды клиентов (пресеты #65, включая «Власник») не распространяется — им 2FA опциональна после MVP. Риск GitHub-организации «второй владелец без 2FA» закрыт 2026-08-22 и к этому пункту не относится |

## Наши, до этапа домена

| # | П | Вопрос |
|---|---|---|
| 36 | П1 | Где живёт фоновый воркер (опрос статусов НП, ежедневные снимки остатков, счета): Vercel Cron, отдельный сервис или очередь. Позиция alex: Vercel Cron → `/api/cron/*` + таблица прогонов джобов; Redis не появляется. `[contract]` — финализируется со Step, issue #68 |
| 37 | П1 | `expand/contract` для миграций: без этого откат деплоя при уже применённой миграции ломает прод |
| 38 | П2 | Хранилище фото (фотоотчёт, акты расхождений) — **Vercel Blob**, подписанные URL, загрузка с клиента мимо лимита тела 4,5 МБ |
| 39 | П2 | Транзакционная почта: счета и акты уходят клиенту — **Resend**, домен подтверждается DNS-записями |
| 40 | П2 | Ключ шифрования учётных данных интеграций (ключи НП, токены CRM) — прикладной ключ AES-256-GCM в env (локально `~/.skladplus/env`, на Vercel — env), версия ключа хранится в шифротексте, ротация — скрипт перешифровки под новую версию |
| 41 | П2 | Бэкапы Neon и **репетиция восстановления**. Бэкап, который ни разу не разворачивали, бэкапом не является |
| 43 | П3 | Идемпотентность вебхуков CRM и Новой Почты: повторная доставка не должна порождать второй заказ |
| 67 | П1 | `ShipperProfile.isDefault` без ограничения: правило «последний использованный становится типовым» при параллельных обновлениях оставляет два типовых профиля. Решение: partial unique по `accountId` при `isDefault = true`, смена — транзакцией |
| 68 | П1 | Упаковка (#60) вне журнала остатков: `Packing.onHand` хранит остаток, а `StockMovement` знает только `productId`. Приход и списание упаковки в журнал не попадут, `onHand` разойдётся с аудитом. Решение: общий журнал — `packingId?` в `StockMovement`, заполнен ровно один из product/packing |
| 69 | П1 | `StockSnapshot` без уникальности `(accountId, date)`: повторный прогон воркера (#36) создаст второй снимок за ту же дату и исказит среднее, по которому считается хранение (#2). Нужны `@@unique` и `upsert` |
| 70 | П1 | `InventorySession` без `accountId`: режим «клиент» не задаёт арендатора, и `ADJUSTMENT` может смешать товары разных клиентов. Нужны `accountId` в сессии и проверка соответствия у строк |
| 71 | П1 | Уточнение к #43: `Order.externalId` необязателен внутри `@@unique(accountId, channel, externalId)`. Postgres считает `NULL` различными, поэтому событие без внешнего идентификатора уникальность не нарушит и создаст второй заказ. Решение: `externalId` обязателен на границе интеграций — вебхук без него отвергается 400 |
| 72 | П1 | `linkedType?`/`linkedId?` в `SupportThread` и `ServiceRequest` — не внешний ключ: ни существование цели, ни совпадение её `accountId` не проверяются, запись может указывать на удалённую или чужую сущность. Решение: типизированные FK — `orderId?`/`supplyId?`/`returnId?`, заполнен ровно один; распространяется и на #80 |
| 73 | П1 | `Setting.scopeId` необязателен внутри `@@unique(scope, scopeId, key)`: строки уровня `PLATFORM` с одним `key` дублируются, потому что `NULL` различны. Решение: `scopeId` обязателен, для `PLATFORM` — пустая строка |
| 74 | П1 | `TariffVersion` не запрещает пересечение периодов: две версии, действующие на одну дату, дают для клиента разные цены. Решение: exclusion constraint (btree_gist) в миграции — решение общее для #74, #78, #79 |
| 75 | П3 | **`pnpm audit` не доказывает, что свежей уязвимости нет.** Записи уровня репозитория попадают в глобальную базу GitHub Advisory с задержкой, а сканер читает именно её: на релизе первых дней «чисто» значит «ещё не приехало». Обновление по security-поводу сверяется с публикациями апстрима, а не только со сканером |
| 76 | П1 | `Barcode` объявляет `@@unique(accountId, code)`, но поля `accountId` у таблицы нет: ограничение ссылается на несуществующий столбец, схема не соберётся. Решение: завести `accountId` — код уникален в пределах аккаунта, поиск однозначен |
| 77 | П1 | `TariffLine` без уникальности `(tariffVersionId, serviceType)`: две строки одной услуги в одной версии тарифа дают неоднозначную цену. Исключение — `CUSTOM`, где повтор осмыслен, поэтому ключ частичный |
| 78 | П1 | `Invoice` не запрещает пересечение `periodFrom`/`periodTo` для одного клиента: два счёта на один период — двойной биллинг. Решение — exclusion constraint, как в #74 |
| 79 | П1 | `CommissionRate` не запрещает пересечение `effectiveFrom`/`effectiveTo` — тот же класс, что #74: две ставки на одну дату дают разный расчёт комиссии. Решение — exclusion constraint, как в #74 |
| 80 | П1 | `OrderStatusEvent.actorId?`, `ReturnComment.authorId`, `SupportMessage.authorId` — не внешние ключи. Решение — FK на `User`, как в #72 |
| 81 | П2 | Состав запроса и ответа `InternetDocument.getDocumentPrice` официальной документацией не подтверждён: `developers.novaposhta.ua` отдаёт 403. Сверить в кабинете разработчика до задачи 2.11 — от него зависит, что показывает вартість доставки на панели ТТН |

## Отложено при настройке Neon

| # | П | Вопрос |
|---|---|---|
| 44 | — | **Провайдер аутентификации — закрыт: better-auth**, строка-решение в `docs/06-architecture.md`. Открытым остаётся одно условие: транзакционная почта (#39) нужна до регистрации, восстановления пароля и подтверждения адреса — нитка их не делает, вход по заведённой учётке писем не шлёт. Остальные условия — в строке-решении. Требования безопасности — `docs/02-product-spec.md` §8 |
| 45 | П2 | **Ключ MCP отозвать или переселить перед первым боевым клиентом.** Сейчас он ограничен проектом Skladplus, и этого достаточно, пока в базе синтетика. С появлением реальных данных нужен отдельный проект-песочница: Neon не умеет ограничивать ключ веткой, только проектом |
| 46 | П3 | **Имя базы в Neon — `neondb`, локально и в CI — `skladplus`.** Расхождение косметическое: имя входит в строку подключения, которая и так своя в каждой среде. Решение: выровнять — база в Neon переименовывается в `skladplus` |
