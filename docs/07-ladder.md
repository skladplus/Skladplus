# 07 — Лестница: ступени, источники, сверка

Метод: какие ступени проходит проект, как каждая называется в индустрии и что из
неё уже лежит в репозитории. Кто и что делает — `08-workplan.md`, решения —
`06-architecture.md`, отложенные вопросы — `09-findings-backlog.md`.

`#N` — пункт `docs/09-findings-backlog.md`; ссылка на GitHub идёт
со словом: `PR #23`, `issue #16`.


## Ступени и источники

| # | Ступень | Как называется в индустрии |
|---|---|---|
| 0 | Discovery | Inception (RUP) · Lean Inception (Caroli) · Event Storming (Brandolini) |
| 1 | Спецификация | PRD · сценарии использования (Cockburn) · design doc в культуре Google/RFC |
| 2 | Домен | DDD strategic design (Evans, Vernon): ограниченный контекст, единый язык, инварианты |
| 3 | Системный дизайн | C4 (Brown) · ADR (Nygard) · атрибуты качества и ATAM (SEI) · оценка нагрузки на салфетке |
| 4 | UX | информационная архитектура (Rosenfeld & Morville) · Atomic Design (Frost) · дизайн-токены |
| 5 | Фундамент | Continuous Delivery (Humble & Farley) · 12-factor · trunk-based · DORA (Forsgren) |
| 6 | Вертикальный срез | Walking Skeleton (Cockburn) · Tracer Bullet (Hunt & Thomas) · steel thread |
| 7 | Контексты параллельно | Team Topologies (Skelton & Pais) · закон Конвея · stream-aligned teams |

## Порядок — решений, а не слоёв

Лестница не говорит «сначала бэк, потом фронт». Она задаёт порядок решений:
следующий шаг дёшев только тогда, когда предыдущий закрыт. Открытое решение
не блокирует работу — оно делает её переделываемой, и цена всплывает не сразу.

**Закрыт** значит два условия сразу: решение принято и оно материализовано
в артефакте, который можно проверить. Принятое, но нигде не воплощённое
решение закрытым не считается: тридцать три таблицы, описанные в 04,
не мешают `schema.prisma` быть пустой.

Порядок ступеней как жёсткой очереди источника не имеет — это компоновка,
и сверка ниже показывает, чего она стоит. Ступени 1–4 проработаны почти на полторы сотни
килобайт текста, ступень 6 — ноль строк. Проверить эти килобайты
нечем: ни одна строка спецификации не сталкивалась с работающим кодом.

## Две нитки, и их не надо путать

Слово «нитка» в проекте означает два разных среза, и сделан только первый:

| Нитка | Что доказывает | Состояние |
|---|---|---|
| Инфраструктурная | пустая страница доезжает до прода: CI, миграции, деплой, проверка живости | сделана, ступень 5 |
| Доменная (Cockburn) | данные клиента проходят путь форма → `AccountScope` → Prisma → экран, и чужие не видны | не начата, ступень 6 |

Первая отвечает на вопрос «конвейер работает?», вторая — «архитектура держит
предметную область?». Зелёный деплой пустой страницы про вторую не говорит
ничего.

Порядок работ — **walking-skeleton-first**, и у него тоже есть источники:

- **XP** (Beck). Проект узнаёт правду о своей архитектуре из работающего кода.
  Проектирование идёт приращениями, а не одним заходом до начала работ.
- **Lean** (Poppendieck). Незавершённая работа — запас. Документ, не проверенный
  работающим срезом, — запас, который обесценивается: в бэклоге больше двух
  десятков пунктов П1 и П2, меняющих схему или поведение: часть ждёт ответа
  заказчика, остальные — нашего решения.
- **Shape Up** (Singer). У ступени аппетит, а не оценка. Кончилось время —
  режется объём, а не срок; иначе ступень 1 растёт ровно столько, сколько её пишут.

Отсюда порядок:

1. Ступени 0–4 доводятся до уровня, которого хватает построить нитку, и не глубже.
2. Ступень 5 пройдена — она и делает нитку проверяемой на каждом коммите.
3. Ступень 6 идёт сразу за ней — раньше полного каркаса интерфейса, раньше
   полной схемы, раньше полной аутентификации.
4. Дальше движение идёт срезами. Каждый добирает из 0–4 ровно то, что нужно ему,
   и оставляет за собой работающий продукт.

## Сверка с репозиторием

| # | Состояние | Чем подтверждается | Чего не хватает |
|---|---|---|---|
| 0 | вне репозитория | материалы заказчика и расшифровка встречи лежат рядом с репозиторием; в git их нет по запретам 3 и 6 | итога discovery своими словами: пользователи, события домена, что вне рамок |
| 1 | сделано | `02-product-spec.md` — роли, матрица «роль × действие», карта экранов, машины статусов, SLA, финансовый контур | `01-overview.md`, на который ссылаются `README.md` и `AGENTS.md`, не существует |
| 2 | наполовину | `04-domain-draft.md` — три контекста, таблицы, сквозные конвенции, индексы | карты контекстов со связями, инвариантов отдельным списком, единого языка одним словарём: сейчас он размазан по 02 §1, 04 и `AGENTS.md` |
| 3 | решение принято, структура не материализована | `05-system-design.md` — C4, узкие места, оценка нагрузки, атрибуты качества с мерами (§6); ADR ведётся одним журналом `06-architecture.md` сознательно | из описанной структуры в коде есть только три пакета воркспейса: ни `apps/web/modules/` с реестром, ни `packages/db/src/repositories/`, ни подмодулей `core` |
| 4 | только на бумаге | `03-ux-ui.md` — информационная архитектура, конструктор модулей, инвентарь состояний, порядок реализации | компонентов и экранов: Tailwind 4 с токенами стоит (PR #32), но `app/page.tsx` — по-прежнему заголовок и абзац |
| 5 | сделано | десять job CI с одним обязательным `ci-ok`, миграции против настоящего Postgres, gitleaks, проверка авторства, автодеплой из `main` | метрик DORA не снимаем — при двух разработчиках это преждевременно |
| 6 | **инфраструктурная нитка есть, доменной нет** | пустая страница доезжает до прода, `/api/health` отвечает `200` с живой базой | `schema.prisma` без единой таблицы, `zod` не объявлен ни в одном пакете, роутов два — доменная нитка не начата |
| 7 | расписано | `08-workplan.md` — этапы, владельцы, зависимости, ритм | старт параллельной работы упирается в ступень 6 |

## Что мешает ступени 6

Пустая `schema.prisma` — записанное решение: бизнес-правила не закрыты, и схема,
написанная в тот момент, была бы переписана. Решение своё отработало —
конвейер миграций прогнан на пустой схеме и работает.

Дальше оно начинает стоить: пока в схеме нет таблиц, спецификация растёт
непроверенной, а первая же нитка вскроет в ней ошибки, которые сейчас никак
не видны. Нитка не требует закрытых бизнес-правил — она требует одного узкого
пути, где правила известны.

## Правило против отката в документы

Документ дописывается срезом, который его проверил, а не наоборот. Раздел
спецификации, ни разу не прошедший через работающий код, считается черновиком
независимо от объёма — и правится по факту, а не защищается.
