# Журнал — alex (@yennned)

Запись после каждого локального коммита: дата · ветка · хеш · что сделано ·
что дальше · открытые вопросы. Формат из NovaPostBot, проверен на 4800 строках.

---

## 2026-08-28 · chore/alex-core-purity · граница packages/core под проверкой

**Сделано:** шаг CI в job `guards` вместо обещания в `AGENTS.md`. Ловит импорт,
ре-экспорт, `require` и динамический `import` react/next/prisma/`@skladplus/db`
в исходниках `packages/core` и отдельно проверяет манифест — зависимость
приезжает и без импорта в `src`.

**Проверено делом:** прогнал восемь случаев. Ловятся `import type … from 'react'`,
`export * from 'next/headers'`, `await import('react')`, `require('@prisma/client')`,
`import … from '@skladplus/db'` и `next` в манифесте. Не ловятся и не должны:
посторонний `nextcloud-client` и упоминание React в комментарии — хвост шаблона
требует кавычку или слэш, а начало — `from`/`require`/`import(`.

**Отдельно про пустую проверку:** шаг падает, если список файлов пуст.
Исчезнувшие исходники иначе выглядели бы как соблюдённое правило — ровно так же,
как это уже сделано в job `authorship`.

**Формулировка расширена:** `AGENTS.md` обещал только React и Next, а 03 §4
требует «core не знает о базе и фреймворке». Проверять половину правила смысла
нет — правило и проверка сведены к одному тексту, документ и комментарий
в `index.ts` поправлены тем же PR.

**Дальше:** мерж #32 (зависимости первыми), затем задача 1.2 — better-auth.

## 2026-08-28 · chore/alex-ui-deps · Tailwind 4 и shadcn/ui, задача 1.1 нитки

**Сделано:** стек интерфейса отдельным маленьким PR до кода экранов.
Tailwind 4 PostCSS-плагином, дизайн-токены одним файлом `app/globals.css`
(нейтральная основа + семантика состояний, тема одна светлая),
`components.json` и CLI shadcn в devDependencies, `cn()` в `lib/utils.ts`.

**Проверено делом, а не зелёной сборкой:** сборка проходит и с пустым CSS,
поэтому проверял выдачу. В `.next` — 8789 байт стилей, `--warning: #efad32`
и остальные токены на месте, `.text-muted-foreground` скомпилирован
в `color: var(--muted-foreground)`, все четыре класса страницы сгенерированы.
`pnpm exec shadcn --version` отвечает `4.19.0` — компоненты добавляются
без `npx` и `dlx`.

**Что всплыло:** Biome по умолчанию считает `@theme` и `@apply` синтаксической
ошибкой и роняет `lint`. Лечится `css.parser.tailwindDirectives` в `biome.json`,
а не исключением файла из проверки — исключённый файл перестал бы
форматироваться вместе с остальными.

**Сознательно не ставил:** `lucide-react` и `class-variance-authority`.
Потребителя у них пока нет, а придут они сами с первым компонентом,
которому нужны, — тем же `shadcn add`.

**Дальше:** задача 1.2 — better-auth, затем 1.3 первая настоящая миграция
(`Account`, `AccountMembership`, `StaffMember`, `Product`).

## 2026-08-28 · docs/alex-ladder · лестница 00, этап 1 переписан в нитку

**Сделано:** `docs/00-ladder.md` — восемь ступеней с индустриальными названиями
и источниками, сверка каждой с репозиторием, порядок работ
walking-skeleton-first вместо прохождения ступеней по очереди.

`docs/07-workplan.md` — этап 1 «Фундамент» переписан в нитку «Товар завёлся»:
вход, создание товара, список у клиента, тот же товар у склада, чужой товар
не виден. Девять задач вместо шести, и каждая нужна срезу.

**Что показала сверка:** ступени 1–4 проработаны на сто восемьдесят килобайт,
ступень 6 — ноль строк. `schema.prisma` без единой таблицы, `app/page.tsx` —
три строки текста. Проверить написанное нечем: ни одна строка спецификации
не сталкивалась с работающим кодом.

**Побочный выигрыш от резки:** нитка не шлёт писем, поэтому транзакционная
почта (#39) перестала блокировать аутентификацию — она была обязательным
условием #44. Регистрация, восстановление и подтверждение приходят
следующими срезами вместе с почтой.

**Решения опросника:** провайдер аутентификации — better-auth (#44 закрыт);
Tailwind + shadcn/ui ставится до нитки отдельным PR; задачи 1.5 и 1.6 режутся
до нужного срезу.

**По ревью:** число открытых пунктов заменено проверяемым — тринадцать П1 и П2,
пометки «заблокировано» в бэклоге нет вовсе. «До полной схемы» переписано на
«раньше»: читалось как требование и переворачивало смысл. DoD задачи 1.9 получил
субъекта — без него он запрещал складу видеть чужой товар и ломал задачу 1.8.
Условие про rate limiting убрано из #44: оно уже стоит строкой-решением в 02.

**Дальше:** задача 1.1 — Tailwind + shadcn/ui отдельным маленьким PR первым,
затем better-auth и первая настоящая миграция.

## 2026-08-28 · docs/alex-doc-conventions · соглашения по документам

**Сделано:** в `AGENTS.md` — раздел «Соглашения по документам»: состояние
вместо истории · решение строкой «что · почему · статус · дата» в 02 ·
обоснование только то, без которого правило нарушат · прозаический абзац
не длиннее шести строк.

В CI (job guards) — два шага: «Документы — состояние, а не хроника»
(заголовки с датой, «по факту» или «уточнением» в docs/, `AGENTS.md`,
`README.md`) и «Абзацы не распухли» (все `*.md`, журнал включительно).
Те же правила — в `path_instructions` `.coderabbit.yaml` для ревью прозой.

Приведено к зелёному: абзацы в 03 (правкой в ветке PR #29), 04, 06
и в журнале; заголовки-хроники в 02 и findings-backlog переименованы,
даты ушли в текст.

**Дальше:** мерж после PR #29 (ребейз подтянет починенный 03); PR помечен
`[contract]` — ждёт, пока второй на связи.

## 2026-08-28 · docs/alex-system-design · системный дизайн 03, статус в журнале 02

**Сделано:** `docs/03-system-design.md` — системный дизайн: прикидка нагрузки
«на салфетке» (~3,6 млн строк движений/год, чтение/запись ≈ 50:1) · C4 в три
уровня mermaid · контексты A–E со слоями чистоты и правилом межконтекстных
вызовов · структура папок с пометками готового · паттерны с обоснованием ·
согласованность/доступность/кэш/back pressure · узкие места · опросник §8 из
пяти вопросов.

Документ прогнан через аудит по system-design-primer и локальное ревью:
из 12 кандидатов порог не прошёл никто, три подпороговые правки внесены
(#36 помечен рекомендацией, оговорка про nullable `externalId`, честная
фраза про отсутствующий CI-шаг). Абзацы длиннее шести строк разрезаны
по мыслям или переведены в списки — под соглашение по документам
(отдельный `[contract]`-PR).

`docs/02-architecture.md` — журнал решений приведён к канону четырёх колонок:
добавлен «Статус»; «Neon MCP видит только ветку с синтетикой» — «отменено»
(границы «ветка» в Neon нет, работает project-scoped ключ); дополнено
обоснование миграций; строка-решение про `docs/03`. PR #29.

**Дальше:** ответ CodeRabbit и ревью Степана по PR #29, затем ответы на
опросник §8 — №1 воркер (#36) и №2 auth (#44) решаются вдвоём, `[contract]`.

**Открытые вопросы:** AGENTS.md обещает CI-проверку чистоты `packages/core`,
шага в workflows нет — завести с каркасом (задача 1.5), формулировку поправить
отдельным `[contract]`-PR; импорт Excel 10 МБ (04 §4.2.5) против лимита тела
4,5 МБ на Vercel — путь через хранилище, §8 №3–4.

## 2026-08-26 · docs/alex-workplan · рабочий план 07

**Сделано:** `docs/07-workplan.md` — четыре этапа с зависимостями и владельцами:
фундамент (схема идентичности, StaffScope, каркас UI-конструктора, auth — alex,
step параллельно готовит свой контекст и возвращает смоук-тесты) → два контекста
параллельно (товары/поставки/остатки — step; замовлення/збірка/повернення —
alex) → финансы/чат/воркер → чек-лист перед первым боевым клиентом. Каждая
задача = будущий issue; распределение утверждает Степан ревью этого PR.

**Дальше:** мержи PR #24–#28 по мере ответов CodeRabbit, затем issues по
этапу 1 и старт фундамента.

**Открытые вопросы:** согласие Степана с распределением контекстов; выбор
auth-провайдера (#44) — решение вдвоём.

## 2026-08-26 · docs/alex-ux-ui · UX/UI и архитектура-конструктор 06

**Сделано:** `docs/06-ux-ui.md` — навигация двух контуров · архитектура-
конструктор (реестр модулей, реестр виджетов, настройки-как-данные с
`SettingDefinition`+zod; честная граница: config-driven, не no-code) · базовые
паттерны (раздел-таблица, двухколоночная форма, setup-блокеры, сканер-паттерн,
пустые состояния, опасные действия).

Там же: инвентарь состояний контрола как норма DoD UI-задач · дизайн-система
(украинский, токены, Tailwind + shadcn/ui — зависимости отдельным PR) · фазы
реализации A/B/C · таблица «что чиним у референса».

**Дальше:** `docs/07-workplan.md` — распределение работ; мержи оставшихся PR.

## 2026-08-26 · docs/alex-domain-draft · черновик доменной схемы 05

**Сделано:** `docs/05-domain-draft.md` — 33 таблицы пятью блоками: идентичность
и доступ (агностично к провайдеру #44) · товары/поставки/остатки с журналом
движений как источником истины · заказы/возвраты/сборка (18 статусов, SLA-паузы,
двухстадийный финиш) · поддержка/настройки (чат, Setting под конструктор) ·
финансы (версионные тарифы строками, цена в проводке, комиссии со снимком
ставки, счета постоплаты). Отдельно: что сознательно не закладываем и какие
ответы заказчика могут поменять структуру.

**Дальше:** `docs/06-ux-ui.md`, `docs/07-workplan.md`; мержи оставшихся PR
после ответа CodeRabbit.

## 2026-08-26 · docs/alex-product-spec · продуктовая спецификация 04

**Сделано:** `docs/04-product-spec.md` — полная продуктовая спека: глоссарий ·
роли и границы данных (owner / manager / client_owner / client_member с тремя
пресетами) · 9 матриц «роль × действие» (✓/○/— со сносками-ограничениями) ·
27 карточек экранов трёх контуров (аутентификация, кабинет клиента, склад) ·
6 машин статусов таблицами «статус · enum · кто переводит · откуда · эффекты»
(заказ — 18 статусов с двухстадийным финишем SLA).

Там же: SLA и приоритеты · финансовый контур постоплаты с таблицей проводок ·
безопасность аутентификации · таблица расхождений с видео заказчика. Формат
утверждался на срезе «Замовлення», потом заполнено остальное.
В `docs/02-architecture.md` — строка-решение о втором примитиве `StaffScope`
для кросс-аккаунтных выборок персонала.

**Дальше:** `docs/05-domain-draft.md` (черновик схемы), `docs/06-ux-ui.md`
(UX/UI и конструктор), `docs/07-workplan.md` (распределение работ на двоих).

**Открытые вопросы:** прежние 4 подтверждения заказчика (↷) + состав отчётов
владельца (#30) — его рабочее место, проектировать без него нельзя.

## 2026-08-26 · docs/alex-backlog-transcript · решения опросника в бэклог

**Сделано:** разобраны три источника — транскрипт видео заказчика от 2026-08-22,
структура референсной системы (словесно, материалы вне git по правилу №3) и
существующий бэклог. Решения по открытым продуктовым вопросам зафиксированы
опросником, четыре подтверждения заказчика остаются открытыми (↷):
в `docs/findings-backlog.md` новый раздел «Зафиксировано опросником 2026-08-26»
(#22–25 перенесены из «Требует ответа», добавлены #47–66), уточнены #19 (постоплата
подтверждена), #21 (ролей сборщика/руководителя сборки не будет — всё на менеджерах),
#44 (рекомендация better-auth). Заказчику остаются 4 подтверждения, помечены ↷.

**Отдельно:** PR #23 закрыт по решению; документы 00/01/03 из него в зависимостях
не участвуют — карта продукта будет в 04/05/06 следующими PR. ПДн третьих лиц
в локальных материалах референс-аудита вычищены (скриншот заявок удалён,
email/телефоны заменены шаблонными).

**Дальше:** PR с `docs/04-product-spec.md` (карта экранов, матрица «роль ×
действие», статусные машины), затем `docs/05-domain-draft.md` и `docs/06-ux-ui.md`.

**Открытые вопросы:** 4 подтверждения заказчика (↷ в бэклоге): цены типов
возвратов · проценты комиссий менеджеров · украинские подписи статусов ·
постоплата вместо баланса из видео.

## 2026-08-22 · main · этап 0, каркас

**Сделано:** корневой скелет монорепо (pnpm 10.34.5, Node 24, TypeScript 7.0.2,
Biome 2.5.10), deny-by-default `.gitignore`, `AGENTS.md` как канон правил,
`scripts/context.sh` как единый ввод в контекст, `.claude/settings.json`
с `includeCoAuthoredBy: false` и deny-правилами.
Удалён профиль браузера с чужой живой сессией (куки и Login Data).

**Дальше:** организация `skladplus`, перенос репозитория, приглашение @Stepandj.

## 2026-08-22 · chore/alex-github · #1 · CI и правила

**Сделано:** десять джобов под агрегатом `ci-ok`, экшены пинованы SHA
(`sha_pinning_required` включён), actionlint и gitleaks — бинарями с проверкой
sha256. CODEOWNERS, dependabot на npm и github-actions, шаблоны PR и issue
с обязательным правилом «только ID», `.coderabbit.yaml`. Ruleset на `main`
с `bypass_actors: []`.

**Проверено боем, не на бумаге:** прямой push в `main` отбит
(`push declined due to repository rule violations`) · merge-коммит и rebase
отклонены, squash прошёл · squash-сообщение — одна строка заголовка, без трейлеров ·
ветка удалена автоматически · синтетический PR #2 с `Co-Authored-By: Claude` уронил
`ci-ok`, диапазон посчитан верно · `gh pr merge --squash --admin` по красному CI
отклонён. PR #2 закрыт, в `main` не попал.

**Дальше:** организация, перенос, @Stepandj.

**Открытые вопросы:** ждут ответа заказчика — см. `docs/findings-backlog.md`.

## 2026-08-22 · feat/alex-skeleton · #4 · приложение и слой доступа

**Сделано:** Next 16 с `/api/health`, `packages/db` с Prisma 7 и обёрткой
`forAccount(accountId)`, `packages/core` без React и Next, docker-compose
на порту 5433, `scripts/bootstrap.sh`, 13 плагинов в project-scope,
чистка скиллов (13 security → 3).

**Проверено:** health отдаёт 200 с реальным запросом в базу и 503 без утечки
хоста · `db:diff` даёт 2 при дрейфе и 0 после отката · `prisma generate`
работает без переменных окружения · `migrate deploy` идемпотентен ·
7 контрактных тестов на разграничение клиентов.

**Что поймал CI, а не я:** сгенерированный клиент Prisma нужен каждому джобу,
а не только сборке. Локально `typecheck` и `unit` проходили на клиенте
от прошлых запусков.

**Организация:** `skladplus` создана, репозиторий перенесён, @Stepandj владелец.
Перенос сбросил secret scanning и push protection — возвращено, плюс выставлены
org-настройки по умолчанию. Доска Projects с пятью колонками привязана к репозиторию.

**Дальше:** CodeRabbit App, Neon, Vercel, Sentry — только через браузер.
2FA у @Stepandj — самое слабое место защиты.

## 2026-08-22 · chore/alex-neon · Neon

**Сделано:** проект Skladplus во Франкфурте на Postgres 18, Launch. Ветка
`production` защищена, автоскейл снижен 8 → 1 CU. Ключ MCP ограничен проектом,
лежит в `~/.skladplus/env`, в конфиг записана переменная.

**Что поймал:** боевая база NovaPostBot в той же организации Neon, поэтому
OAuth для MCP открыл бы агенту чужой прод. Онбординг Neon сам прописал такой
сервер в user-scope — удалён. Ветка как граница доступа в Neon не существует
вовсе, только проект.

**Своя ошибка:** первая попытка выпуска ключа напечатала его в вывод. Отозван
и перевыпущен.

**Дальше:** Vercel и интеграция Neon↔Vercel до первого preview. Сессию нужно
перезапустить — `NEON_API_KEY` появится в окружении из `~/.zshrc`.

## 2026-08-22 · Vercel и миграции прода

**Сделано:** сборка Vercel починена (генерация клиента Prisma), регион функций
переведён `iad1` → `fra1`, конфиг принимает имя `DATABASE_URL_UNPOOLED`
от интеграции Neon, заведён workflow миграций прода.

**Проверено делом:** `/api/health` отдаёт **200** и на preview, и на проде,
база доступна. Интеграция сама завела ветку `preview/fix/alex-direct-url`
в Neon. Все владельцы организации с 2FA.

**Что нашёл проверкой, а не по экрану:** функции подняло в Вашингтоне при базе
во Франкфурте — каждый запрос ходил бы через Атлантику. Интеграция называет
прямое подключение иначе, чем ждал конфиг. Боевая ветка Neon была пуста —
механизма доставки миграций на прод не существовало вовсе.

**Дальше:** задачи Степана (#11–#13), домен, снятие защиты с production
перед первым клиентом.

## 2026-08-22 · Первое ревью CodeRabbit и его первое замечание

**Сделано:** CodeRabbit ответил на PR #22 — тумблер на репозиторий в его дашборде
закрыл диагноз из `docs/02-architecture.md`. Единственный тред разобран:
команда проверки прода печатала тело ответа, не глядя на статус.

**Проверено делом:** тот же запрос с заведомо неверным секретом против
защищённого preview этого PR. Старая команда напечатала HTML страницы входа
Vercel и вышла с кодом **0**; новая — `HTTP 302` и код **1**.

**Суть:** `fetch` в Node сам идёт по редиректу, поэтому проверка обхода защиты
молча выдавала за успех ровно тот случай, который должна была ловить.
Лечится `redirect:"manual"`, проверкой `r.ok` и `process.exitCode` в `catch`.

**Дальше:** дождаться закрытия треда и мержа PR #22 — по новому правилу
раньше ревью не мержим. Затем задачи Степана (#11–#13) и домен.
